# VEH Debugger 업그레이드 방향

## 구현 범위 결정 (2026-09-11)

아이디어는 삭제하지 않고 책임과 성숙도에 따라 다음처럼 배치한다.

- **관측 엔진 핵심**: register state delta, indirect branch profiling, memory write trace,
  executable-page write detection, memory region classification, exception trace, conditional trace
- **제한된 의미로 구현**: snapshot/restore는 선택 메모리와 thread context만 다루는
  `checkpoint`로 제공하며 프로세스 전체 상태 복원을 보장하지 않는다.
- **분석 계층 담당**: loop/dispatcher folding과 persistent-register 추론은 DLL이 제공하는
  block/edge hit 통계를 바탕으로 MCP/VM Analyzer에서 수행한다.
- **실험 기능**: 모든 memory read의 값 추적과 taint-lite는 성능·정확성 검증 전까지
  opt-in 실험 기능으로 유지한다.

구현은 별도 semantic trace primitive를 복제하기보다 기존 `veh_trace_basic_blocks`의
수집 옵션과 압축 결과를 확장한다. 모든 단계는 x86/x64, 직접 MCP 호출, `veh_batch`,
breakpoint action에서 동일한 결과 의미를 유지해야 한다.

### 단계별 완료 조건

1. **Phase 1**: register delta, 자동 indirect target 집계, hot block/edge 요약
2. **Phase 2**: memory write before/after, executable write와 후속 실행의 연결
3. **Phase 3**: memory region 분류, 조건부 수집, exception event 확장, analyzer folding
4. **Phase 4**: 명시적 한계를 가진 checkpoint create/restore/diff/delete
5. **Phase 5**: opt-in memory read trace와 제한된 dependency/taint 실험

현재 진행 상태:

- [x] Phase 1 — register delta, indirect target, hot block/edge
- [x] Phase 2 — bounded memory write before/after, executable write→execute 연결
- [x] Phase 3 — region 분류, 조건부 수집, exception 확장, analyzer folding
- [x] Phase 4 — bounded session-local checkpoint create/restore/diff/delete
- [x] Phase 5 — opt-in bounded memory-read trace와 최대 32개 명시적 source의 conservative dependency bitset. 정확히 모델링하지 못하는 REP/16바이트 초과/FS·GS operand는 unsupported로 보고하며, filtered collection gap은 `dependency_incomplete`로 노출한다.

각 Phase는 bounded storage, truncation 보고, Release x86/x64 빌드, 통합 테스트,
한·영 문서 및 `CHANGELOG` 갱신을 완료해야 종료된다.

## 목표

현재 `veh-debugger`는 일반적인 디버거 기능을 넘어, AI 에이전트가 직접 제어할 수 있는 동적 분석 엔진에 가깝다.

향후 VM 난독화, 패커, 보호 코드, 자체 가상머신 분석까지 범용적으로 활용하려면 디버거 내부에 VM 전용 로직을 과도하게 넣기보다는 다음 역할에 집중하는 것이 좋다.

> **정확하고 빠르게 런타임 상태를 관측하고, 대량의 trace를 압축해서 제공하는 엔진**

VM 구조 추론, handler 의미 분석, VIP/VSP 판별 등은 별도의 분석 계층에서 수행한다.

---

# 전체 구조

```text
AI Agent
   │
   ▼
veh_batch
   │
   ├─ veh_trace_basic_blocks
   ├─ veh_trace_calls
   ├─ veh_trace_region
   ├─ snapshot / restore
   ├─ memory trace
   └─ exception trace
   │
   ▼
veh-debugger.dll
   │
   ├─ 대량 single-step
   ├─ register 변화 추적
   ├─ memory access 추적
   ├─ indirect branch 기록
   ├─ exception 기록
   └─ trace 압축
   │
   ▼
VM Analyzer
   │
   ├─ dispatcher 탐지
   ├─ VIP / VSP 추정
   ├─ handler 분리
   ├─ VM opcode 의미 추정
   └─ CFG 복구
```

`veh_batch`는 orchestration 용도로 사용하고, 실제 수십만~수백만 instruction의 처리는 DLL 내부에서 수행하는 것이 중요하다.

MCP를 instruction 단위 반복 호출에 사용하는 구조는 피한다.

---

# 우선순위

| 우선순위 | 기능                            |
| ---- | ----------------------------- |
| S    | Memory Read/Write Trace       |
| S    | State Delta                   |
| S    | Indirect Branch Profiling     |
| S    | Snapshot / Restore            |
| S    | Loop / Dispatcher Folding     |
| A    | Self-Modifying Code Detection |
| A    | Memory Region Classification  |
| A    | Exception Trace 강화            |
| A    | Conditional Trace             |
| B    | Taint-lite                    |
| B    | VM 구조 자동 추정 보조 정보             |

---

# 1. Memory Read / Write Trace

현재 basic block / edge trace에서 가장 크게 보강할 부분이다.

각 instruction 또는 block이 어떤 메모리를 읽고 썼는지 기록한다.

## 필요한 정보

```text
address
read / write
size
old value
new value
instruction RIP
```

예:

```text
Block 0x140123450

Reads:
  [R12+0x18]
  size: 8
  value: 0x140800000

  [R13]
  size: 4
  value: 0x1234

Writes:
  [R13-8]
  size: 8
  before: 0x00000000
  after:  0x00001234
```

이 정보는 이후 다음 분석에 매우 중요하다.

```text
VIP 후보 탐지
VSP 후보 탐지
VM context 탐지
handler 효과 추론
self-modifying code 탐지
```

---

# 2. State Delta

매번 전체 레지스터 상태를 반환하지 말고 이전 상태와 비교해서 변화한 항목만 기록한다.

예:

```text
Before:
R12 = 1000
R13 = 2000
RAX = 10

After:
R12 = 1002
R13 = 1FF8
RAX = 30
```

결과:

```json
{
  "register_delta": {
    "r12": {
      "before": "0x1000",
      "after": "0x1002"
    },
    "r13": {
      "before": "0x2000",
      "after": "0x1ff8"
    },
    "rax": {
      "before": "0x10",
      "after": "0x30"
    }
  }
}
```

메모리도 동일한 방식으로 처리한다.

```json
{
  "memory_delta": [
    {
      "address": "0x12340000",
      "size": 8,
      "before": "0x10",
      "after": "0x20"
    }
  ]
}
```

VM 분석에서는 절대값보다 `before -> after` 변화가 더 유용하다.

---

# 3. Indirect Branch Profiling

다음과 같은 instruction의 실제 목적지를 자동 수집한다.

```asm
jmp rax
jmp rcx
jmp [rax+rcx*8]
call rdx
call [table+rax*8]
```

결과 예:

```text
Instruction:
0x140123400

Type:
indirect jmp

Targets:
0x140200100  1823 hits
0x140200380   441 hits
0x140201020    72 hits
```

VM dispatcher가 handler table을 사용하는 경우 이 정보만으로도 handler 후보를 상당수 식별할 수 있다.

---

# 4. Snapshot / Restore

분석 중 특정 실행 상태를 저장하고 다시 복원할 수 있게 한다.

예상 API:

```text
veh_snapshot_create
veh_snapshot_restore
veh_snapshot_diff
veh_snapshot_delete
```

최소 저장 대상:

```text
registers
selected memory regions
thread context
```

사용 예:

```text
snapshot 생성

input = 1
실행
결과 저장

snapshot 복원

input = 2
실행
결과 저장

두 실행 결과 비교
```

이 기능이 있으면 실제 프로세스에서도 differential execution이 가능하다.

Unicorn으로 옮기기 전 단계에서도 handler 의미 분석에 매우 유용하다.

---

# 5. Loop / Dispatcher Folding

VM trace에서는 dispatcher와 공통 loop가 반복해서 등장한다.

예:

```text
A
B
C
D
B
E
B
F
B
G
```

여기서 `B`가 dispatcher라면 원시 trace 대신 다음처럼 압축한다.

```text
Dispatcher:
B

Handlers:
C
D
E
F
G
```

초기에는 VM이라는 개념까지 넣을 필요 없이 다음 정도만 구현해도 된다.

```text
repeated block detection
hot block detection
dominant re-entry block detection
loop folding
```

VM-specific 판단은 후처리기에서 한다.

---

# 6. Self-Modifying Code Detection

실행 가능한 메모리 영역에 쓰기가 발생하면 기록한다.

예:

```text
Executable page modified

Page:
0x140900000

Changed:
0x140900120 - 0x140900178

Before hash:
...

After hash:
...
```

그리고 이후 해당 영역이 실행되면 연결한다.

```text
WRITE executable page
        ↓
EXECUTE modified range
```

패커, JIT형 보호기, 동적 handler 생성형 VM 분석에서 중요하다.

---

# 7. Memory Region Classification

모든 주소를 가능한 경우 자동 분류한다.

예:

```text
image
module
heap
stack
private RW
private RX
private RWX
mapped
guard
```

예를 들어:

```text
R12 -> heap RW
R13 -> heap RW
RIP -> private RX
```

가 지속적으로 관찰된다면 VM context와 VM code 영역을 추정하기 쉬워진다.

---

# 8. Exception Trace 강화

현재 exception edge 기능을 확장한다.

한 exception event에 다음 정보를 담는다.

```text
exception code
faulting RIP
faulting address
handler address
continuation address
register state
register delta
stack snapshot
```

예:

```json
{
  "type": "access_violation",
  "fault_rip": "0x140123456",
  "fault_address": "0x0",
  "handler": "0x140800000",
  "continuation": "0x140123490"
}
```

예외 기반 dispatcher나 보호기 분석에 특히 유용하다.

---

# 9. Conditional Trace

Trace 시작/종료 조건을 지원한다.

예:

```text
start when:
R12 == 0x1234

stop when:
RIP outside module

collect only when:
[R13] != 0
```

예상 형태:

```json
{
  "start_condition": "R12 == 0x1234",
  "stop_condition": "RIP < 0x140100000 || RIP > 0x140200000"
}
```

불필요한 trace 양을 크게 줄일 수 있다.

---

# 10. Taint-lite

완전한 symbolic taint engine까지 구현할 필요는 없다.

레지스터와 메모리에 간단한 origin bitset 정도만 유지한다.

예:

```text
Input:
RDX
```

실행:

```asm
mov rax, rdx
xor rax, rcx
mov [r12+30h], rax
```

결과:

```text
[R12+0x30]

depends on:
RDX
RCX
```

또는:

```text
branch at 0x140123456

depends on:
VM stack value #2
```

정도의 정보만 제공해도 분석 가치가 크다.

---

# 권장 신규 Trace Primitive

범용적인 고급 trace 기능 하나를 추가하는 것을 권장한다.

이름 예:

```text
veh_trace_region_semantic
```

입력 예:

```json
{
  "threadId": 1234,

  "start": "0x140123000",
  "end": "0x140130000",

  "max_steps": 500000,

  "collect": {
    "blocks": true,
    "edges": true,
    "indirect_targets": true,
    "memory_reads": true,
    "memory_writes": true,
    "register_delta": true,
    "exceptions": true
  },

  "compression": {
    "fold_repeated_blocks": true,
    "dedup_state": true
  }
}
```

출력 예:

```text
Trace Summary

Instructions:
481,221

Unique Blocks:
431

Unique Edges:
802

Hot Blocks:
0x1401246A0  18342 hits
0x140124700  18102 hits

Indirect Branches:
0x1401246D3
  unique targets: 72

Persistent Registers:
R12 99.8%
R13 99.5%

Memory Movement:
[R12+0x18] frequently incremented
[R13] frequently moves ±8

Executable Writes:
none

Exceptions:
Access Violation: 231
Unique continuation targets: 4
```

이 정도 결과만 있어도 외부 VM Analyzer가 구조를 상당히 쉽게 추정할 수 있다.

---

# debugger 안에 넣지 않는 것이 좋은 기능

다음 기능은 `veh-debugger` 자체보다 별도 분석 계층에서 처리하는 것이 좋다.

```text
dispatcher 최종 판정
VIP 판정
VSP 판정
VM architecture 판정
handler opcode 분류
VM opcode naming
virtual CFG reconstruction
VTIL 변환
Unicorn replay
Z3 검증
pseudo-code 생성
```

이유는 `veh-debugger`가 특정 VMProtect/Themida 구조에 종속되는 것을 막기 위해서다.

---

# 권장 역할 분리

## veh-debugger

담당:

```text
runtime observation
trace
memory
registers
exceptions
indirect branches
snapshot
state delta
trace compression
```

목표:

> Windows Runtime Instrumentation Oracle

---

## VM Analyzer

담당:

```text
dispatcher detection
handler segmentation
VIP/VSP inference
VM state reconstruction
handler semantics
virtual CFG
```

---

## Unicorn

담당:

```text
handler replay
input mutation
differential execution
semantic experiments
```

---

## VTIL

담당:

```text
semantic IR
CFG
optimization
dead-code elimination
constant folding
devirtualized representation
```

---

# 구현 추천 순서

## Phase 1

우선 구현 가치가 가장 높은 기능:

```text
1. Memory read/write trace
2. Register / memory state delta
3. Indirect branch target profiling
```

이 세 개만 추가해도 현재 trace 활용도가 크게 올라간다.

---

## Phase 2

```text
4. Snapshot / Restore
5. Loop folding
6. Memory region classification
7. Self-modifying code detection
```

여기까지 되면 VM 및 패커 분석 엔진으로 상당히 강해진다.

---

## Phase 3

```text
8. Conditional trace
9. Exception trace 확장
10. Taint-lite
```

---

# 최종 방향

`veh-debugger` 자체가 VMProtect 전용 devirtualizer가 되어서는 안 된다.

대신 다음과 같은 구조를 목표로 한다.

```text
실제 프로그램 실행
       ↓
veh-debugger
       ↓
정확한 런타임 관측
       ↓
압축된 trace / state 변화
       ↓
별도 분석 엔진
       ↓
VM 구조 및 의미 추론
```

이렇게 하면 동일한 디버거 기반을 다음 분야에 모두 재사용할 수 있다.

```text
VM 난독화
패커
프로텍터
자체 제작 VM
게임 클라이언트 분석
프로토콜 분석
런타임 암호화 분석
self-modifying code
예외 기반 난독화
```

핵심 원칙은 다음 두 가지다.

> **VEH debugger는 최대한 정확한 관측기여야 한다.**

> **의미 해석은 별도 analyzer가 담당해야 한다.**
